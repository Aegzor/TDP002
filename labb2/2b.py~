def shopping_list():
    print(the_list)
    mode = input('Type a command.Type help for information: ')

def shopping_add():
    the_list.append(input("What do you wish to add? "))
    mode = input('Type a command.Type help for information: ')
    

the_list = []
mode = input('Type a command. Type help for information: ')
while mode != 'q':
    if mode == 'help':
        print('Write the letter in parantheses\nShow List (show)\nAdd object to list(add)\nDelete object from list (del)\nEdit object in list (edit)\nquit program (q)')
        mode = input('Type a command.Type help for information: ')
    elif mode == 'show':
        shopping_list()
    elif mode == 'add':
        shopping_add()
    elif mode == 'del':
        shopping_remove()
    elif mode == 'edit':
        shopping_edit()
    else:
        input('Input a proper command: ')
